---
description: Add, commit, and message for VCS
---

Follow these steps when checking code into the repository:

1. Verify that `post_coding_checks.md` has been run and passed.
2. Stage all relevant files.
3. Determine a concise, atomic commit message explaining *why* the changes were made.
// turbo-all
4. `git add .`
5. `git commit -m "[Your specific commit message]"`
